// Dynamically load stripe.js straight from stripe,
var stripejsSrc = "https://js.stripe.com/v2/"
$.externalScript(stripejsSrc).done(function(script, status){
	console.log("Load stripe.js:" + status);
	Stripe.setPublishableKey("<%= Rails.application.secrets.stripe_public_key %>");
})