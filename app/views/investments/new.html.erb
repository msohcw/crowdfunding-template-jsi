<h2>Back <%= Project.find(@project).name %></h2>
<hr />
<%= form_for(@investment) do |f| %>
	<%= f.label :amount %>
	<%= f.number_field :amount %>
	<%= f.hidden_field :project_id, value: @project %>
	<%= f.submit "Back!", class: "button button-primary" %>
<% end %>